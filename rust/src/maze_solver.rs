pub struct Point {
  x: i32,
  y: i32,
}

pub fn solve<'a>(maze: &'a [&str], wall: &'a str, start: Point, end: Point) -> &'a [Point] {
  return &[];
}

#[cfg(test)]
mod tests {
  
  #[test]
  fn test() {
    assert_eq!(true, false);
  }
}